# ZZMI Mod 管家 · 更新名单（v1.5.5 → v1.5.11）

> 这些版本都**还没发到 GitHub**，一共攒在一起。测试包已打好：
> `安装包\ZZMI-Mod-Manager-Setup-1.5.11.exe`（一步到位，含全部改动）。
>
> ⚠ **从 v1.5.10 起只发安装包，不再单独发便携版 exe**（安装包自带程序，少一份文件、少一次 UAC）。

---

## v1.5.11 —— 深色弹窗看不清修复 + 卡片直接重命名（本次重点）

### ① 深色主题下输入弹窗文字看不清 —— 已修

**你遇到的**：浅色主题弹窗正常，深色主题下「新的文件夹名:」说明文字和输入框里的字
全是黑的，完全看不见。

**原因**：弹窗文字误用了 `--ink`（那是荧光绿**按钮上的黑字**用的颜色）。浅色主题下
它恰好接近正常黑字所以看不出来；深色主题下弹窗是深底，黑字就隐身了。

**现在**：改用主题文字色 `--txt`，深浅两套主题都清晰。

### ② mod 卡片新增「✎ 重命名」按钮

以前重命名要先点「详情」→ 再按「✎ 重命名」，两步。现在**卡片底部直接有
「✎ 重命名」按钮**（在「详情」和「只留这套」中间），一点就弹输入框。
和详情里的入口共用同一套逻辑：名字被占用仍会自动加序号并弹窗说明。

---

## v1.5.10 —— 界面一关程序真退出 + 自制输入弹窗 + 只发安装包

### ① 关掉界面窗口，程序真的退出了（解决你「装不上」的根因）

**你遇到的**：关掉浏览器界面后，真正的程序（那个被隐藏的命令窗口）还在后台跑、
占着文件锁，导致新安装包覆盖不了、还杀不掉（它是提权进程，普通方式拒绝对）。

**现在**：界面窗口一关，程序会在几秒后**自动退出**；刷新页面（F5）不会误杀。
具体机制：页面加载时向后端打 `/api/hello`「我还活着」，关页面时 `pagehide` 打
`/api/bye`「我要走了」→ 后端 5 秒后自动退出。所以**以后更新前只要关掉界面窗口，
程序就会自己消失，安装包就能正常覆盖**。

### ② 输入弹窗换成自制窗，标题是「ZZMI MOD 管家」

以前那些「输入名字」的框是浏览器原生 `prompt()`，标题栏显示的是网址、没法改。
现在统一用自制弹窗，标题就是「ZZMI MOD 管家」。涉及：新建文件夹、新建仓库、
给方案起名、变体改键输入、重命名 mod。

### ③ 更新提醒加了注意事项 + 只发安装包

- 发现新版本时，提醒条里写明「**更新时请先关闭本程序**」，避免装不上
- **从本版起不再单独发便携版 exe**，只发安装包（自带程序，少一份文件、少一次 UAC）

---

## v1.5.9 —— 修掉「同名 mod 启不了」+ 绝区零图标（本次重点）

### ① 同名两套，禁用的那套启用了会失败 —— 已修

**你遇到的**：已经启用了一个「蜜西皮肤」，同时还有 `DISABLED_蜜西皮肤`（禁用），
点它的开关想启用 → 报「完成 0 项, 1 项失败: · DISABLED_蜜西皮肤: 找不到目录(可能已被移动)」。

**根因**：同名两套在软件内部是靠 `#1` 这种编号区分的，v1.5.8 的自动加序号
只处理了「改过去撞名」，没处理「编号条目本身找不到路」——是两件事，上一版确实漏了。

**现在**：

| 操作 | 结果 |
|---|---|
| 启用 `DISABLED_蜜西皮肤`（「蜜西皮肤」已启用） | ✅ 启用为「蜜西皮肤 (2)」+ 弹窗说明 |
| 启用/禁用已启用那套 | ✅ 正常操作，**不会认错套** |
| 再撞一次 | ✅ 接着排 (3)，不叠成「(2) (2)」 |

### ② 图标全部换成绝区零官方图标

从游戏 exe 里提取的官方图标，统一用到三处：

- **exe 文件本身**和命令行窗口
- **安装包**
- **界面窗口 / 任务栏** —— 你截图里那个「地球」图标，就是浏览器应用窗口没配
  网页图标时的默认样子，现在随程序带上了绝区零图标

---

## v1.5.8 —— 重名自动加序号 + 弹窗说明

**以前**：改名或启停时，目标名字已被占用就直接报错。
**现在**：**永远不报错，自动在后面加 `(2) (3) …`**，和 Windows 复制文件一个逻辑。

- 弹窗写清「你想叫 X → 实际叫 X (2)」，不满意就地重命名即可
- 批量操作 / 套用方案时一次性列出所有被改名的项（超过 12 个折叠显示）
- 弹窗明确说明「**你原来的文件夹一个都没动过**」
- 覆盖：单个启停、批量启停、套用方案、单独重命名、子目录启停、散装 ini 启停

---

## v1.5.7 —— 命令窗口不再碍眼

- 黑色命令窗口在界面起来后 **1.8 秒自动隐藏**
- 排错时可叫回：设置弹窗里「▣ 显示命令窗口(看日志)」按钮
- 日志同时写进 `%USERPROFILE%\.zzmi-manager\zzmi.log`（超 512KB 自动轮转）
- 逃生口：启动参数 `--console`（或 `-c`）、环境变量 `ZZMI_KEEP_CONSOLE=1`

---

## v1.5.6 —— 改名 / 启停失败时，手把手教你

- 弹教程窗说清失败原因（**权限被拒** / **正被占用** / 其它）
- 4 步图文教程 + 「一键复制 DISABLED_」按钮
- 「去改名禁用 MOD」按钮：打开文件夹并选中它，窗口顶到最前，F2 改名
- 单个/批量/方案启停失败全都接了这套弹窗

---

## v1.5.5 —— 仓库真正由你自己掌握

- 「添加仓库」：新建（ZZMI 目录下）或锁定任意盘的任意文件夹
- 仓库列表分三组：ZZMI 目录下的 / 锁定的其它位置 / 自定义文件夹
- 找不到仓库时空态直接给添加按钮
- 外置仓库与 Mods 双向搬运；删除仓库只移出列表，不删磁盘文件

---

## 怎么测试

1. 关掉正在运行的旧版本：**界面窗口一关，程序会自动退出**（几秒内）。
   如果之前那种「看不见又杀不掉」的残留还在，用任务管理器结束 `ZZMI-Mod-Manager.exe` 即可
2. 双击 `安装包\ZZMI-Mod-Manager-Setup-1.5.10.exe` 装（需要管理员权限）
3. 重点测这几条：
   - **造你截图那个现场**：「蜜西皮肤」启用 + `DISABLED_蜜西皮肤` 禁用，
     点禁用那套的开关 → 应该启用成功并变成「蜜西皮肤 (2)」+ 弹窗
   - 互相改名/启停再撞几次 → 依次排 (3) (4)，不出现「(2) (2)」
   - **看图标**：桌面快捷方式、任务栏窗口、安装包是不是都是绝区零图标了
   - 命令窗口 1.8 秒自动消失；设置里能叫回来
   - 仓库管理 → 加一个别的盘的文件夹当仓库
