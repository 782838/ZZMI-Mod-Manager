# 连拍 / 界面 五个问题 —— 原因分析 · 影响范围 · 修复方案

> 对应版本：当前 v1.5.36（`zzmi_manager.py` / `ui.html`）
> 结论先行：**5 个问题里 4 个是管家自身的真 bug（已定位到具体代码行），第 4 个（11155）不是管家产生的。**
> 其中问题 1 与问题 3、问题 5 三者**同源**——都指向「滚动缓冲 + 前台冻结」这套抓拍模型。

---

## 结论速览

| # | 问题 | 是否管家 bug | 根因一句话 | 改动量 |
|---|---|---|---|---|
| 1 | 第二次按侧键，下方胶片条不变 | ✅ 是 | 管家界面在前台时缓冲**被冻结**，第二次按只是把**同一批帧**重新打包，前端其实刷新了、但内容一模一样 | 中（与 #3 一起改） |
| 2 | 3 秒只出 36 帧，各档数量要翻倍 | ✅ 是（参数问题） | 抓屏帧率 12fps × 3s = 36 帧；翻倍需 24fps，而实测抓屏上限约 23fps，**必须先给抓屏路径减负** | 小~中 |
| 3 | 录制时长不生效、时长持续变化 | ✅ 是 | `photo_seconds` 只被当成**帧数上限**用，从没当成**时间窗口**；滚动缓冲的帧数是固定 36，实际跨度 = 36÷实际帧率，**随负载漂移** | 中 |
| 4 | 每次任务后报错 11155 服务异常 | ❌ 不是 | 仓库内**零遥测/上报代码**、全仓搜不到 11155；与官方文档里模型侧错误码（11133/14003）同族，指向**客户端/模型服务侧** | 无法在管家内修 |
| 5 | 切回管家应显示挑帧页，实际停在主界面 | ✅ 是 | 管家窗口不在时按 F9 会**新开一个浏览器窗口 = 全新页面加载**，首帧轮询把刚抓的那批当成「旧的」播种掉（`_pbShown`），于是**不弹** | 小 |

---

## 问题 1：第二次点侧键，仅上方刷新、下方胶片条始终无变化

### 原因分析

**先排除前端。** 我写了 jsdom 行为测试（已固化为 `tests/ui_burst.js` 场景 12），模拟「挑帧页开着时又来一批**形状不同**的新帧」：提示、档位按钮、胶片条、大图**全部正确重绘**（41/0 通过）。所以**前端渲染逻辑没问题**。

**真因在后端：管家界面在前台时，滚动缓冲被「冻结」而不是「清空」。**

`zzmi_manager.py` 抓屏循环：

```python
if _is_manager_foreground():
    self._stop.wait(0.5)
    continue            # ← 只是跳过抓屏, ring 里那批帧原封不动留着
```

而 `trigger()` 每次都是**快照整个 ring**：

```python
def trigger(self):
    with self.lock:
        snap = list(self.ring)      # ← 冻结时, 拿到的还是同一批帧
```

于是第二次按侧键会发生什么：

1. 第一次按侧键 → 后端把管家界面**顶到最前**（`_raise_manager_window_bg`）→ 管家成了前台 → **缓冲从此冻结**。
2. 用户再按一次侧键 → `trigger()` 快照到的还是**那 36 帧一模一样的内容**，只是 `burst_id` 递增了。
3. 前端收到新 `burst_id`，**确实重新渲染了**；但 `pbShow()` 会把档位重置回 1 档、位置重置到该档最后一帧 → **大图跳到了另一帧 → 看起来「上方刷新了」**。
4. 胶片条里的帧号虽然变了，但**图像内容逐张相同** → 视觉上「下方界面始终无变化」。

这同时解释了「**清除缓存无效**」：`clear_cache()` 确实清空了 ring，但管家还在前台，ring **依然是空的**（不会重新录），所以下一次按侧键只会得到空状态 —— 用户当然觉得"没用"。

> 佐证：后端自己的提示文案也承认这个模型 ——
> `_why_empty()` 里写着「管家界面正开着, 缓冲是冻结的(免得录到管家自己)」。

### 影响范围

- 所有「**在管家界面可见时按抓拍键**」的场景（包括：挑帧页开着时连按、设置页开着时按、以及第一次按完之后接着再按）。
- 第一次按（管家不在前台时）**正常**。
- 与问题 3、问题 5 同源：都是这套「滚动缓冲 + 前台冻结」的副作用。

### 修复方案

**方案 A（推荐，与问题 3 一起改）：把「滚动缓冲」换成「按下即录」。**
按下瞬间开始录 N 秒，录完立刻拆帧。此时「管家在前台就冻结」这条规则**整个删掉**，问题 1 自然消失。

⚠️ 关键连带改动：现在 `_after_press_bg()` 会在按下后**立刻把管家顶到最前**。如果改成"按下后录 3 秒"，这 3 秒就会录到**管家自己**。所以必须把顶窗**推迟到录制结束之后**。

**方案 B（保守，保留"回溯"语义）：只放宽冻结条件。**
- 挑帧页/设置页开着时**不冻结**（用户在看帧，游戏在后面继续跑）；
- 或者：冻结时**清空 ring 并给出明确提示**，而不是留一批旧帧让用户误以为"抓到了"。
- 至少要让「同一批帧被重复打包」这件事**可见**：如果 `trigger()` 拿到的帧与上一批完全相同，就在提示里写「这批和上一批一样：管家在前台时缓冲被冻结了」。

**建议**：直接上方案 A —— 它一次把问题 1 和问题 3 都解决掉，代码反而更简单。

---

## 问题 2：3 秒只拆出 36 帧，各档数量要翻倍

### 原因分析

档位（金字塔）现在是 `TIERS = (4, 8, 16, 32)`，第 5 档 = 全部帧：

```python
TARGET_FPS = 12                              # 抓屏目标帧率
TIERS = (4, 8, 16, 32)                       # 4 个中间档, 第 5 档 = 全部
cap = max(6, int(self.TARGET_FPS * secs))    # 12 × 3 = 36 帧
```

你截图里的 `1档·5张 / 2档·8张 / 3档·16张 / 4档·32张 / 5档·36张` 正是 **36 帧**跑出来的（1 档固定多保留最新一帧，所以是 5 而不是 4）。

**「每一档翻倍」等价于「3 秒内要拿到约 72 帧」，也就是帧率要 12 → 24fps。**

**我实测了这台机器的抓屏能力（1920×1080）：**

| 阶段 | 耗时（中位） |
|---|---|
| `ImageGrab.grab()` | **29.3 ms** ← 瓶颈 |
| 16×16 差异哈希 | 7.6 ms |
| JPEG 编码（quality 82） | 6.3 ms |
| **合计** | **43.2 ms → 上限约 23 fps** |

- 12fps 预算 83.3ms → 只用了 38%，**很宽裕**（所以现在能稳定出 36 帧）。
- 24fps 预算 41.7ms → 实测 p95 已经 **46.1ms**，**超预算** → 直接提到 24fps 会**拿不满 72 帧**，只能到 ~23fps（约 69 帧），而且游戏运行时（GPU 占用高）会更慢。

### 影响范围

- 只影响连拍帧数与档位密度；不影响其他功能。
- 内存/CPU：36 帧 → 72 帧，JPEG 约 179KB/帧 → 内存占用约翻倍（36×179KB≈6.4MB → 12.9MB），可接受。

### 修复方案

**先把抓屏路径减负，再提帧率**（否则提上去也拿不满）：

1. **把 16×16 哈希挪出抓屏循环**（省 7.6ms/帧）。
   哈希只被 `_thin()` 建金字塔时用，改成在 `trigger()`（按下那一刻）**惰性计算**：
   - 抓屏线程只剩 `grab + encode` ≈ 35.6ms → 上限约 **28fps**，24fps 才有余量。
   - 代价：按下后建金字塔多花 ~0.5 秒（72 帧 × 7.6ms）。这正好符合你说的「**随后完成图片拆解**」。
2. **JPEG 编码降到 1600×900**（6.3ms → 3.0ms，体积 319KB → 229KB）。
   截图用途完全够，还能顺带降低内存与传输量。
3. **帧率 12 → 24**，`TIERS` 由 `(4, 8, 16, 32)` 改为 `(8, 16, 32, 64)`。
4. **优雅降级**：机器跟不上时，实际帧数少，档位自动按现有帧数收缩（`_thin()` 已经是这个行为），不会报错。
5. 给设置页补一句真实帧率显示（例如「当前实际 ≈ 21 fps」），避免用户以为没生效。

> 如果实测在游戏里仍拿不满 72 帧，**备选**：把「3 秒」放宽到 4 秒（帧数 ×1.33）或直接允许 24fps 拿多少算多少 —— 但这就跟问题 3 的「固定 3 秒」冲突，需要你拍板。

---

## 问题 3：录制时长疑似未生效，缓存中的时长持续变化

### 原因分析

**`photo_seconds` 从来没被当成「时间」用过，它只是一个「帧数上限」。**

```python
def _grab_one(self):
    secs = self._secs()                                  # 3
    cap  = max(6, int(self.TARGET_FPS * secs))           # 12 × 3 = 36 帧
    self.ring.append((ts, jpeg, hb))
    if len(self.ring) > cap:
        del self.ring[:len(self.ring) - cap]             # 按**帧数**裁剪, 不是按时间戳
```

后果：

1. **实际跨度 = 36 ÷ 实际帧率**。帧率随负载波动（实测 12fps 预算下只用 38%，但游戏里会变慢），所以同一批 36 帧可能跨 1.6 秒，也可能跨 3.6 秒 —— 这就是你看到的「**缓存中的时长持续变化**」。
2. **缓冲是持续滚动的**，没有「开始/结束」的概念：它一直在录，永远保留最近 36 帧。所以不存在"从某一刻起录 3 秒"。
3. **管家界面在前台时缓冲冻结**（见问题 1），此时"时长"干脆不再变化，而内容停在旧画面上。
4. `_secs()` 每抓一帧都重读配置，改设置时上限**中途改变**，跨度进一步漂移。

### 影响范围

- 所有抓拍批次的**实际时长不可预期**；
- 与问题 1（冻结）叠加后，连按两次会拿到同一批帧；
- 用户在设置里改「回溯时长」时，感受不到确定的效果。

### 修复方案

**方案 A（推荐，贴合你的原话）：「按下即录，录满 N 秒就停」。**

- 侧键按下 → 立刻起一个**定时录制**任务：从按下瞬间开始抓帧，抓满 `photo_seconds`（默认 3）秒就**立即停止**；
- 停止后**立刻建金字塔拆帧**，然后弹挑帧页；**不再有任何额外动作**（不继续录、不重复顶窗）；
- 取消滚动缓冲（`_loop` 只在录制期间抓屏）→ 平时 CPU/内存占用降到 0，也不会再录到管家自己；
- 顶窗必须**推迟到录制结束之后**（否则会录进管家界面，见问题 1 的连带改动）。

**方案 B（保守）：保留滚动缓冲，但改成「按时间戳裁剪」。**
把 `cap` 那套换成「丢弃 `ts < now - secs` 的帧」，这样窗口**严格是 N 秒**，不受帧率影响。语义仍是「抓按下**之前**的 N 秒」。
- 优点：不动交互模型，改动最小；
- 缺点：帧数仍随帧率变化（与问题 2 的"各档翻倍"目标需要配合调帧率）。

**建议**：如果你要的就是「按下 → 录 3 秒 → 拆帧」这个确定流程，走**方案 A**；如果只是嫌时长不准，走**方案 B**。

---

## 问题 4：每次任务执行完毕后出现「错误码 11155，服务异常」

### 原因分析

**这不是管家产生的。** 证据：

1. **全仓检索 `11155`：零命中**（含源码、`ui.html`、脚本、文档）。
2. **管家没有任何错误上报 / 遥测代码**：搜 `上报 / 错误码 / errcode / sentry / telemetry` 在项目源码里零命中（只有 `buildenv` 里第三方库的同名变量，与业务无关）。管家的"日志"只写本地 `zzmi.log` 和 `journal.jsonl`，**不联网上报**。
3. 错误码家族对不上：管家自己用的是 `RegisterHotKey` 的 `err=1409`、`ERROR_ALREADY_EXISTS=183`、`SHFileOperation` 的 `DE_*` 这类系统码，没有 5 位数的业务码。

**它更像是宿主客户端 / 模型服务侧的错误码。** 官方故障排查文档里，模型侧状态码是 `11133 / 14003 / 1001`，频率限制是 `6003`，网络类是 `3002/3003/400/401/504` —— **`11155` 与 `11133` 同族**，指向模型/服务侧。

「**每次任务执行完毕都出现**」这个**必然性**很关键：偶发的网络抖动不会每次都命中，这更像是**任务收尾时的某个固定步骤**（例如会话收尾/统计/上报调用）稳定失败。

### 影响范围

- 不影响管家功能（管家不读这个码，也不受它影响）；
- 影响的是**宿主客户端（WorkBuddy）**的使用体验：任务虽然完成，但会弹错误。
- **不能在管家仓库里修**。

### 修复方案（可执行的部分）

1. **确认来源**：下次出现时截下**完整报错原文** + 出现位置（聊天区 / 任务列表 / 弹窗 / 右下角 toast）+ 时间点。这决定了后续怎么报。
2. **客户端侧**：
   - 升级到最新客户端（这类收尾步骤的失败常常在新版本里已修）；
   - 左下角头像 → **帮助与反馈 → 意见反馈**，**勾选「上传日志」**，附报错原文 + 截图 + 客户端版本 + 复现步骤。这是官方唯一能定位它的途径。
3. **规避**：如果是自动化任务里的收尾失败，在流程里加「失败等 30 秒重试 1 次，仍失败则跳过并记录」，避免它把整个任务标红。
4. **排除干扰**：确认报错与管家**无关**的做法 —— 关掉管家（`⏻ 退出 ZZMI 管家`）后跑一个任务，如果 11155 照旧出现，就与管家彻底无关。

> 如果你能给我 11155 的**完整原文截图**，我可以再帮你判一次是客户端 UI 层还是模型服务层。

---

## 问题 5：从游戏切回管家应显示挑帧页，实际始终停在主界面

### 原因分析

**根因：管家窗口不在时，按 F9 会新开一个浏览器窗口 = 一次全新页面加载，而首帧轮询会把"刚抓的那批"当成旧的吞掉。**

链条如下：

1. 侧键按下时，后端只做「**把已存在的窗口顶到最前**」：
   ```python
   def _raise_manager_window_bg():
       if not _raise_manager_window():
           log("抓拍: 没找到管家界面窗口, 没法自动顶到最前 …")   # 窗口不在 -> 只写日志, 不新开
   ```
   所以**窗口被关掉时，按侧键不会打开任何窗口**。

2. 用户按 F9 想呼出界面 → `toggle_manager_window()`：
   ```python
   hwnd = find_manager_window()
   if not hwnd:
       url = open(os.path.join(DATA_DIR, "last_url.txt")).read().strip()
       if url:
           open_app_window(url, ...)      # ← 新开浏览器窗口 = 全新页面加载
       return
   ```

3. 新页面的首帧轮询里有这么两行（本意是"刷新页面别把旧的那批又弹一遍"）：
   ```js
   _pbShown  = (st.photo_burst||{}).burst_id || 0;   // ← 把刚抓的那批标记成"已展示过"
   _pressSeen = st.photo_press_seq || 0;             // ← 连"按过侧键"也一起吞掉
   ```
   → 刚抓的批次被当成"旧的"，**挑帧页不会弹**，界面停在主界面。

**这就解释了为什么是「始终」**：只要你习惯在打游戏时把管家界面关掉，那么**每一次**按侧键 + F9 呼出，都是这个路径 → 每次都停在主界面。

（如果管家窗口一直开着、只是被游戏盖住，这条链是好的 —— jsdom 测试里「设置页开着 / 照片墙开着 / 遮罩上盖着」都能正确弹出。）

**次要隐患**：`$("#mask").onclick = closeAll` —— 窗口刚获得焦点时的那一下点击如果落在遮罩上，会把刚弹出的挑帧页关掉。这条也可能贡献"切回来只剩主界面"的观感。

### 影响范围

- 所有「**先关掉管家界面 → 打游戏 → 按侧键 → 按 F9 呼出**」的流程；
- 所有「**F5 刷新页面**」之后的抓拍批次（同样被 `_pbShown` 播种吞掉）；
- 窗口一直开着的情况不受影响。

### 修复方案

1. **后端给批次加「未确认」标记**：`burst["pending"] = True`；新增 `POST /api/photo_ack`，前端**真正弹出挑帧页之后**回一次确认，后端把 `pending` 置 False。
2. **前端首帧改成按标记决定**：
   ```js
   // 有未确认的批次 -> 照样弹; 已确认过的才播种, 免得刷新页面又弹一遍
   if (st.photo_burst && st.photo_burst.pending) burstForceShow(st.photo_burst);
   else { _pbShown = st.photo_burst.burst_id || 0; _pressSeen = st.photo_press_seq || 0; }
   ```
   这样**新开窗口 / 刷新页面**都会把"还没看过的批次"弹出来，而"看过的"不会重复打扰。
3. **`pending` 要有有效期**：沿用 `BURST_TTL`（10 分钟）或更短（例如 2 分钟），避免用户第二天开管家还弹一批旧帧。
4. **放宽遮罩点击**：窗口刚获得焦点后 ~400ms 内的点击不当作"关闭挑帧页"（或干脆让挑帧页只由 ✕ / 「弃掉关闭」关闭），避免"点一下窗口就把挑帧页点掉"。
5. **顺带**：`find_manager_window()` 现在优先用 `FindWindowW("Chrome_WidgetWin_1", APP_NAME)` 精确匹配标题。建议改成**只用兜底的"标题包含"枚举**，避免标题带后缀（如 `ZZMI Mod 管家 - 个人 - Microsoft Edge`）时匹配不到而误判"窗口不在"、进而**多开一个窗口**（多开又会踩上面第 3 步）。

---

## 建议的实施顺序

三个问题同源，**建议一次性改完再打包**（避免反复重打安装包）：

1. **问题 3 + 1**：把抓拍模型从「滚动缓冲」换成「按下即录 N 秒」（含：顶窗推迟到录制结束、删掉前台冻结分支）。
2. **问题 2**：抓屏路径减负（哈希惰性化 + 编码降分辨率）→ 帧率 12→24 → `TIERS` 翻倍 → 设置页显示实际帧率。
3. **问题 5**：`pending`/`photo_ack` + 首帧判定 + 遮罩点击放宽 + 窗口匹配放宽。
4. 补测试：`ui_burst.js` 加「首帧有 pending 批次要弹」；`_v1535_check.py` 加「按下即录的时长准确性（±10%）」「帧数达到目标」；`ui_check.py` 加静态断言。
5. 全量回归 → 重打 exe + 安装包 → 字节级验证。

**问题 4** 不走这个流程（不在本仓库范围内）。
